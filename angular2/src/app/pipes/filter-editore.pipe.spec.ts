import { FilterEditorePipe } from './filter-editore.pipe';

describe('FilterEditorePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEditorePipe();
    expect(pipe).toBeTruthy();
  });
});
