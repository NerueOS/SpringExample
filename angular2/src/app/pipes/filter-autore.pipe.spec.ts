import { FilterAutorePipe } from './filter-autore.pipe';

describe('FilterAutorePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAutorePipe();
    expect(pipe).toBeTruthy();
  });
});
