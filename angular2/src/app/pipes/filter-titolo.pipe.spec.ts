import { FilterTitoloPipe } from './filter-titolo.pipe';

describe('FilterTitoloPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTitoloPipe();
    expect(pipe).toBeTruthy();
  });
});
